@model Nullam.Pages.Party.EventsPage
@{
    ViewData["SubTitle"] = Model?.TitleForDetailsView;
    ViewData["Title"] = Model?.TitleForDetailsView;
    Layout = "_Layout";
    var itemId = Model?.ItemId?? string.Empty; 
}

<h1>@ViewData["Title"]</h1>

<div>
    <h4>@ViewData["SubTitle"]</h4>
    <hr />

   @RenderBody()

</div>

<h4>Osavõtjate lisamine</h4>

<div>
    <button class="btn btn-primary" onclick="location.href='https://localhost:7017/Persons/Create?handler=Create'" type="button">
         Eraisik</button>
    <button class="btn btn-primary" onclick="location.href='https://localhost:7017/Organizations/Create?handler=Create'" type="button">
         Ettevõte</button>
</div>
<p></p>
<p></p>
<div>
    <a class="btn btn-light"
       asp-page="Index" 
       asp-page-handler = "Index" 
       asp-route-order="@Model?.CurrentOrder"
       asp-route-index="@Model?.PageIndex" 
       asp-route-filter="@Model?.CurrentFilter"
    >Tagasi</a>
</div>